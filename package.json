{
  "name": "whatsapp-automation",
  "version": "1.0.0",
  "main": "worker.js",
  "scripts": {
    "start": "bash start.sh",
    "build": "npm install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "firebase-admin": "^12.1.1",
    "playwright": "^1.45.0"
  }
}
